services:
  - type: web
    name: tredingsystem-nse-proxy
    runtime: node
    plan: free
    region: singapore
    rootDir: src/TradingSystem.NseProxy
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
